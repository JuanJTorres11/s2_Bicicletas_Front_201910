/**
 * Clase que representa una orden
 */
export class Orden {

    /**
     * El id de la orden
     */
    id: number;

    /**
     * La cantidad de bicicletas ordenadas
     */
    cantidad: number;

    /**
     * El costo total de lo ordenado
     */
    costo:number;

    /**
     * La fecha en que se realizo la orden
     */
    fecha: Date;

    // constructor(id:number, cantidad:number, costo:number, fecha:Date){
    //     this.id=id;
    //     this.cantidad=cantidad;
    //     this.costo=costo;
    //     this.fecha=fecha;
    // }
}